package com.rtm.mq.tool.model;

/**
 * Represents a field node in the message specification tree.
 *
 * <p>This is a placeholder interface for the field node model.
 * The actual implementation will be provided in T-004 (Data Models).</p>
 *
 * <p>A field node represents a single field or nested structure in the
 * MQ message specification, including its metadata such as name, type,
 * length, occurrence rules, and child nodes for nested structures.</p>
 *
 * @see MessageModel
 */
public interface FieldNode {
    // Placeholder interface - implementation details defined in T-004
}
